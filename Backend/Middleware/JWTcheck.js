/*
  Here we are checking for the JWT token, which we
  previously stored in the user's cookies when they logged in.
  If the token is present, we extract the unique user ID from the payload,
  fetch the corresponding user data, and redirect the user to the home page.
*/

